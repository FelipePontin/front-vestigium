import React from 'react';
import ReactDOM from 'react-dom/client';
import Vestigium from './Vestigium';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <Vestigium />
  </React.StrictMode>
);
